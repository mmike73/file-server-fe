import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'file-server-ionic-client',
  webDir: 'dist'
};

export default config;
